name: Production deploy
on:
  push:
    branches:
      - main
    tags:
      - "*"
jobs:
  deploy-main:
    name: Deploy to Main
    runs-on: ubuntu-latest
    steps:
      - name: Call deploy
        uses: 0bCdian/resumeJson/.github/workflows/deploy.yml
        with:
          environment: "production"
