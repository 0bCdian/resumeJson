FROM oven/bun:alpine
ARG MODE="production"
WORKDIR /app

COPY . .
RUN apk update && apk add poppler-utils --no-cache
RUN bun install && bun run build
ENV ENV MODE
EXPOSE ${PORT}
CMD [ "bun","run","start" ]



